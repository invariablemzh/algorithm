#include<bits/stdc++.h>
using namespace std;
#define FAST                                                                                                           \
    ios::sync_with_stdio(0);                                                                                           \
    cin.tie(0);                                                                                                        \
    cout.tie(0);

const int N = 5010;
int a[N];
int f[N][N];

void solve(){
    int n,k,r;
    cin >> n >> k >> r;
    for(int i = 1;i<=n;i++) cin >> a[i];
    for(int i = 1;i<=n;i++){
        
    }

}

int main(){
    FAST
    int T; cin >> T;
    while(T--) solve();
}